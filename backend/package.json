{
  "name": "restful-api-backend",
  "version": "1.0.0",
  "description": "Mock REST API server using JSON Server",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "dev": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "keywords": ["rest", "api", "json-server", "mock"],
  "author": "Student",
  "license": "MIT"
}